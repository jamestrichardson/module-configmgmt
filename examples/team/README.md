# Team Example

This example demonstrates how to create an AWX Team using the `team` module.

## Usage

```bash
terraform init
terraform plan -var="awx_password=<your-password>"
terraform apply -var="awx_password=<your-password>"
```
