# Inventory Example

This example demonstrates how to create an AWX Inventory with groups using the `inventory` module.

## Usage

```bash
terraform init
terraform plan -var="awx_password=<your-password>"
terraform apply -var="awx_password=<your-password>"
```
